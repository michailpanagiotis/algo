
/* This function detects items in an array that are the sum of 2 other items in an array
 * @param {Array} array An array of numeric values
 * @return {Array<{Object}>} An array of results in the form { sum: <Number>, parts: [<Number>, <Number>] }
 */
export const detectSums = (array) => {
  return null;
};
